{
    "buildCommand": "npm run vercel-build",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/scanner_ocr/:path*",
            "destination": "/scanner_ocr/:path*"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}